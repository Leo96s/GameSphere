namespace GameSphere_backend.Enums
{
    /// <summary>
    /// Represents the persistent authorization roles available to users.
    /// </summary>
    public enum UserRole
    {
        User,
        Admin
    }
}
